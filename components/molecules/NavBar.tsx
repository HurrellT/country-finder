import NavBarTitle from "components/atoms/NavBarTitle";

const NavBar = () => (
  <nav className="flex bg-white flex-wrap items-center justify-center p-4">
    <NavBarTitle text="Country Finder" />
  </nav>
);

export default NavBar;